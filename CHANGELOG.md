# AI 聊天伴侣 — 版本历史

---

## v1.12.2 (2026-08-28)

### 修复
- **自动记忆停摆**: 修复聊天历史超过 1000 条后自动记忆提取永不触发的问题（`send()` 内读取上限 1000 与指针同步的 5000 不一致，导致轮数被封顶、指针永远追不上）；历史已超 1000 条的用户升级后会自动补触发一次
- **失败丢记忆**: 修复记忆提取 API 失败时静默吞错且指针照样推进、该批对话永久漏提的问题；现在失败不推进指针（下轮对话后自动重试），并弹 toast 提示
- **跨间隔漏提**: 修复一次跨多个提取间隔时中间轮次被跳过的问题；现在从指针处按批次从旧到新追赶（单次最多 5 批），不再漏轮
- **轮数统计**: 轮数统一按用户消息数计算，不再用「消息条数 ÷ 2」近似，孤儿消息不再导致轮数低估

### 新增
- **手动提取范围**: 「批量提取历史记忆」旁新增输入框，可指定提取最近 N 轮（留空或 0 = 全部）；已手动提取的轮次会推进记忆指针，不会被自动提取重复处理
- **对话轮数指示**: 顶部导航栏角色状态旁显示当前对话总轮数的小徽标（如「128 轮」），发送、撤回、清除、导入后实时刷新

### 其他
- **版本号**: 修正 v1.12.1 遗漏的 `APP_VERSION` 更新（原仍为 1.12.0），本次统一为 v1.12.2

---

## v1.12.1 (2026-07-17)

### 修复
- **版本号统一**: 修复版本号多处不一致问题
  - `sw.js` 不再硬编码版本（原为 `1.10`），改为从注册 URL 的 `?v=` 参数读取，与前端 `APP_VERSION` 保持单一来源，SW 缓存名随版本自动更新
  - `android/app/build.gradle` versionName 修正为 `1.12.0`（原为 `1.1`）
  - `capacitor.config.json` versionCode 与 build.gradle 对齐为 `5`
  - 设置页初始版本显示修正为 `v1.12.0`
  - README 中 APK 文件名示例更新为 `AI聊天伴侣-v1.12.0.apk`
- **聊天视图重置**: 修复清除历史、新建对话和导入数据后欢迎页被从 DOM 删除、界面空白的问题
- **设置零值保存**: 修复 Temperature 设为 0、自动记忆设为关闭后重新打开设置显示错误的问题
- **开场白上下文**: 开场白现在会作为 assistant 消息写入聊天记录，后续对话可正确携带该上下文
- **上下文轮数**: 设置中的“轮数”按一问一答两条消息计算，不再误当成消息条数
- **记忆轮次重置**: 清空或新建对话时重置自动记忆进度；导入历史时同步重建进度
- **存储降级**: IndexedDB 不可用时继续使用 localStorage，并同步配置、人设、消息编辑和删除操作
- **记忆提取精简**: 每批对话最多保存一段 60 至 120 字的长期记忆摘要；提示词动态标明本批次实际轮数，无长期价值时返回 `NO_MEMORY` 且不保存
- **提取成本降低**: 记忆提取不再携带全部历史记忆，输出上限从 512 降至 256 tokens
- **撤回记忆同步**: 撤回消息后按剩余对话轮数校正自动记忆指针；撤回或重置期间生成的旧摘要会被丢弃，避免自动记忆停摆或写入过期内容

---

## v1.12.0 (2026-06-25)

### 重构
- **记忆排序从 ID 改为 timestamp**: 所有记忆排序不再依赖 IndexedDB 自增 ID，改为按 `timestamp` 字段在 JS 层排序
  - 显示端：按时间升序（旧→新），编号 #1 #2 #3...
  - AI 端：按时间降序（新→旧），编号递减（最大=最新）
  - 批量提取记忆：按时间降序，编号递减
  - 导出：按时间升序
- 从根本上修复了导入后 ID 序号混乱的问题

---

## v1.11.3 (2026-06-20)

### 修复
- **记忆列表序号**: 显示序号改为递减编号（`#${length-i}`），与 AI 端一致——最旧=#1，最新=最大号，新增记忆自动接在最大号后面

---

## v1.11.2 (2026-06-19)

### 修复
- **AI 记忆编号**: 发送给 AI 的记忆序号改为递减位置编号（最大=最新），确保 AI 正确理解时间顺序

---

## v1.11.1 (2026-06-19)

### 修复
- **记忆列表序号**: 序号改为按显示位置编号（#1、#2…），不再使用数据库自增 ID，确保排序和编号一致

---

## v1.11 (2026-06-18)

### 修复
- **长期记忆显示顺序**: 修复导入后记忆列表仍按倒序显示的问题，现改为时间正序（旧→新）

---

## v1.10 (2026-06-15)

### 修复
- **保存文件功能**: 改用 Capacitor Filesystem 原生插件（含插件 JS 客户端），文件保存到 Documents 目录
- **导入记忆顺序反转**: 导出时改用时间正序，导入后记忆顺序不再颠倒

### 新增
- 版本管理体系：服务端 SW 缓存、导出数据、APK 均标记 `v1.10`
- 设置页底部显示当前版本号
- CHANGELOG.md 版本历史记录
- NGINX 静态部署方案（支持版本号路径）

### 技术
- 引入 `@capacitor/filesystem@8.1.2` 原生插件
- Service Worker 缓存名动态绑定版本号
- 导出 JSON 新增 `appVersion` 字段

---

## v1.0 (2026-05 ~ 2026-06-14)

### 功能
- 可自定义角色人设的 AI 聊天（DeepSeek / OpenAI 兼容 API）
- 自动记忆提取（每 N 轮对话触发 AI 分析）
- 批量提取历史记忆
- IndexedDB + localStorage 双存储
- 流式 API 调用（SSE）
- 深色模式 / 语音输入 / PWA 离线提示
- 角色人设编辑（性格、说话风格、常用语、爱好、背景故事、边界、开场白）
- 对话历史导出 / 导入

### 构建
- PWA + Android Capacitor APK 双形态
- AGP 8.13.0 / Gradle 8.14.3
- Android SDK 36 兼容
